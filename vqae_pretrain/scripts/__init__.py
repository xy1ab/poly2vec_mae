"""Script package for VQAE pretraining entrypoints.

This file exists so static analyzers can resolve imports such as
`vqae_pretrain.scripts.run_pretrain` and sibling relative imports inside the
`scripts/` directory without relying on editor-specific path settings.
"""
